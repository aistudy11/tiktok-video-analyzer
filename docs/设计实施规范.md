# 设计系统实施规范

> **For AI Agent**: 阅读此文档了解如何将 'Titanium' (V7) 和 'Aurora' (V8) 主题整合到 Next.js 项目中。

---

## 🚨 核心原则

### 基于 ShipAny2 底座开发

本项目基于 **ShipAny2 模板** 构建，所有开发必须遵循以下原则：

1. **保持现有架构** - 认证(better-auth)、数据库(Drizzle ORM)、国际化(next-intl) 保持不变
2. **复用现有组件** - 优先使用 `shared/components/ui/` 中的组件
3. **遵循现有路由结构** - 使用 `[locale]/(landing)/` 路由模式
4. **遵循 `.claude/rules/`** - 所有开发必须符合项目规则

### 禁止事项

- ❌ 创建重复功能的组件
- ❌ 绕过现有认证系统
- ❌ 修改数据库核心结构
- ❌ 在 tailwind.config.ts 中定义颜色（使用 CSS 变量）

---

## 1. 设计源文件

设计已在两个静态 HTML 文件中定稿，这是所有 UI/UX 的**绝对参考**：

| 主题 | 文件 | 风格 |
|------|------|------|
| V7 Titanium（钛金版） | `design/v7-fusion-console.html` | 深色、方形节点、静态背景 |
| V8 Aurora（极光版） | `design/v8-aurora-pro.html` | 深色、圆形节点、动态Blob背景 |

---

## 2. 技术约束

| 项目 | 要求 |
|------|------|
| 框架 | Next.js 14 (App Router) |
| 样式 | Tailwind CSS v4（通过 CSS 变量配置，不用 tailwind.config.ts） |
| 主题引擎 | `next-themes`（已安装） |
| 组件库 | 复用 `shared/components/ui/` |

---

## 3. 实施策略

### 3.1 全局主题变量

将 HTML 文件中的硬编码颜色迁移到 `src/config/style/theme.css`：

| 语义化Token | V7 (Titanium) | V8 (Aurora) | 说明 |
|---|---|---|---|
| `--bg-surface` | `#050505` | `#000000` | 页面背景 |
| `--glass-panel` | `rgba(20, 20, 25, 0.5)` | `rgba(255, 255, 255, 0.02)` | 容器背景 |
| `--glass-border` | `rgba(255, 255, 255, 0.1)` | `rgba(255, 255, 255, 0.08)` | 容器边框 |
| `--accent-primary` | `#ffffff`（白/银） | `#0ea5e9`（天蓝） | 主操作色 |
| `--font-display` | `Outfit` | `Outfit` | 标题字体 |
| `--font-body` | `Inter` | `Plus Jakarta Sans` | 正文字体 |

### 3.2 组件架构

**不要**为 V7 和 V8 创建单独的页面，创建**单一自适应组件**。

#### GlassPanel 组件
位置：`frontend/src/themes/default/blocks/video-gen/glass-panel.tsx`

- **Titanium 模式**: 高模糊 (`backdrop-blur-2xl`)，硬边框
- **Aurora 模式**: 轻模糊，柔和阴影，"水晶"质感

#### ScriptTimeline 组件
位置：`frontend/src/themes/default/blocks/video-gen/script-timeline.tsx`

核心逻辑（Hook → 痛点 → 解决方案 → CTA）在 V7/V8 中相同。

**差异处理**：
- V7 使用**方形节点** (`rounded-xl`) + 等宽数字
- V8 使用**圆形节点** (`rounded-full`) + 无衬线数字

**解决方案**：使用 `data-state` 或 CSS 变量条件化节点形状

#### BackgroundLayer 组件
位置：`frontend/src/themes/default/blocks/video-gen/background-layer.tsx`

**逻辑**：
- `theme === 'titanium'`：渲染 V7 的静态背景图
- `theme === 'aurora'`：渲染 V8 的 3 个 CSS Blob 动画

---

## 4. 现有组件复用清单

**必须复用的 ShipAny2 组件**：

| 组件 | 路径 | 用途 |
|------|------|------|
| Button | `shared/components/ui/button` | 所有按钮 |
| Input | `shared/components/ui/input` | 输入框 |
| Textarea | `shared/components/ui/textarea` | 多行文本 |
| Header | `themes/default/blocks/header.tsx` | 页面头部 |
| Footer | `themes/default/blocks/footer.tsx` | 页面底部 |
| ThemeProvider | `core/theme/provider.tsx` | 主题切换 |

**新建组件（放在 `themes/default/blocks/video-gen/`）**：

| 组件 | 说明 |
|------|------|
| GlassPanel | 玻璃态容器 |
| BackgroundLayer | 主题背景层 |
| ScriptTimeline | 脚本时间线 |
| StatsCard | 数据卡片 |
| VideoPreview | 视频预览 |

---

## 5. AI 执行清单

1. [ ] **字体配置** - 在 `layout.tsx` 配置 Outfit, Inter, Plus Jakarta Sans
2. [ ] **CSS变量** - 从 V7/V8 HTML 提取颜色到 `theme.css`
3. [ ] **动画迁移** - 将 `@keyframes float` 和 `@keyframes blob-bounce` 移到全局 CSS
4. [ ] **背景组件** - 创建 `BackgroundLayer` 组件
5. [ ] **玻璃组件** - 创建 `GlassPanel` 组件
6. [ ] **时间线组件** - 将 HTML 结构转为 React 组件
7. [ ] **整合测试** - 验证两套主题正常切换

---

## 6. 关键规则

- ⚠️ **不要发明新颜色** - 严格使用 HTML 文件中的颜色值
- ⚠️ **不要用 tailwind.config.ts 定义颜色** - 使用原生 CSS 变量支持动态主题
- ⚠️ **保留动画** - `@keyframes float` 和 `@keyframes blob-bounce` 必须迁移到全局 CSS
- ⚠️ **复用现有组件** - Button、Input 等使用 ShipAny2 现有组件

---

*文档更新时间：2025-01-25*
